# 后端 Dockerfile
FROM node:20-alpine

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm ci --only=production

# 复制 Prisma schema
COPY prisma ./prisma/

# 生成 Prisma Client
RUN npx prisma generate

# 复制编译后的代码
COPY dist ./dist/

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["node", "dist/main.js"]


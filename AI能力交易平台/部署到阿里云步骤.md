# AI能力交易平台 - 阿里云部署步骤

## 服务器信息
- **公网IP**: 39.97.161.240
- **用户名**: Administrator
- **密码**: AIEC-51talk

## 部署包位置
```
/Users/paranoid/51Talk_Market_Project/AI能力交易平台/ai-platform-deploy.tar.gz
```

---

## 🚀 快速部署（3步完成）

### 第1步：上传部署包到服务器

**方法A - 使用终端命令：**
```bash
# 在Mac终端执行（会提示输入密码：AIEC-51talk）
scp /Users/paranoid/51Talk_Market_Project/AI能力交易平台/ai-platform-deploy.tar.gz Administrator@39.97.161.240:/tmp/
```

**方法B - 使用FileZilla等SFTP工具：**
1. 主机: 39.97.161.240
2. 用户名: Administrator
3. 密码: AIEC-51talk
4. 端口: 22
5. 上传 `ai-platform-deploy.tar.gz` 到 `/tmp/` 目录

### 第2步：SSH登录服务器并初始化环境

```bash
# 登录服务器（输入密码：AIEC-51talk）
ssh Administrator@39.97.161.240
```

登录后执行：
```bash
# 解压部署包
cd /tmp
tar -xzvf ai-platform-deploy.tar.gz
cd deploy-package

# 运行环境初始化脚本（安装Node.js、PostgreSQL、Nginx等）
chmod +x setup-server.sh
./setup-server.sh
```

### 第3步：部署应用

```bash
# 运行应用部署脚本
chmod +x deploy-app.sh
./deploy-app.sh
```

---

## ✅ 部署完成后

### 访问地址
```
http://39.97.161.240
```

### 管理员登录
- 用户名: `admin`
- 密码: `admin123`

### 常用运维命令

```bash
# 查看后端日志
pm2 logs ai-platform-backend

# 重启后端服务
pm2 restart ai-platform-backend

# 查看所有进程状态
pm2 status

# 重启Nginx
sudo systemctl restart nginx

# 查看Nginx日志
sudo tail -f /var/log/nginx/error.log
```

---

## 🔧 故障排查

### 1. 网站无法访问
```bash
# 检查Nginx状态
sudo systemctl status nginx

# 检查后端服务
pm2 status

# 检查端口监听
netstat -tlnp | grep -E "80|3000"
```

### 2. 数据库连接失败
```bash
# 检查PostgreSQL状态
sudo systemctl status postgresql

# 测试数据库连接
psql -h localhost -U aiplatform -d aiplatform
# 密码: AIEC51talk2024
```

### 3. 查看后端错误日志
```bash
pm2 logs ai-platform-backend --lines 100
```

---

## 📋 数据库信息

| 项目 | 值 |
|------|-----|
| 数据库名 | aiplatform |
| 用户名 | aiplatform |
| 密码 | AIEC51talk2024 |
| 主机 | localhost |
| 端口 | 5432 |

---

## 🔐 安全建议（部署后执行）

1. **修改管理员密码**
   - 登录后台后立即修改默认密码

2. **配置HTTPS**
   ```bash
   # 安装certbot
   sudo yum install -y certbot python3-certbot-nginx  # CentOS
   sudo apt install -y certbot python3-certbot-nginx  # Ubuntu
   
   # 申请证书（需要先配置域名）
   sudo certbot --nginx -d yourdomain.com
   ```

3. **配置防火墙**
   ```bash
   # 只开放必要端口
   sudo firewall-cmd --permanent --add-service=http
   sudo firewall-cmd --permanent --add-service=https
   sudo firewall-cmd --reload
   ```

---

## 📞 技术支持

如果部署过程中遇到问题，请检查：
1. 服务器是否有足够的内存（建议至少2GB）
2. 服务器防火墙/安全组是否开放80和443端口
3. 数据库服务是否正常运行

